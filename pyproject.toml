[project]
name = "smart-labelimg"
version = "0.1.0"
description = "LabelImg-style YOLO annotator with smart box suggestions"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
